<% if @microposts.any? %>
<%# if @feed_items.any? %>
  <ol class="microposts">
    <%= render @microposts %>
    <%#= render @feed_items %>
  </ol>
  <%= will_paginate @microposts, :previous_label => ' &lt 前へ', :next_label => '次へ &gt' %>
  <%#= will_paginate @microposts, :previous_label => ' &lt 前へ', :next_label => '次へ &gt' %>
  <%#= will_paginate @feed_items %>
<% end %>