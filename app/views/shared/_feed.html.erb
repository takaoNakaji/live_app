<% if @microposts.any? %>
<%# if @feed_items.any? %>
  <ol class="microposts">
    <%= render @microposts %>
    <%#= render @feed_items %>
  </ol>
  <%= will_paginate @microposts %>
  <%#= will_paginate @feed_items %>
<% end %>